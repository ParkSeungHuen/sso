import 'package:flutter/material.dart';

class SettingCar extends StatelessWidget {
  const SettingCar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
